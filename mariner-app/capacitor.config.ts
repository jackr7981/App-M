import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marinersbd.app',
  appName: 'Mariners of Bangladesh',
  webDir: 'out'
};

export default config;
